/**
 * Student-side school calendar — read-only month view (mirror of parent calendar).
 */

import ParentCalendar from '../parent/ParentCalendar';

// The view itself is identical (read-only school calendar).
// We re-export so the import path stays under the student/ folder.
export default ParentCalendar;
