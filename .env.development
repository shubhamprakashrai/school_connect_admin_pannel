# Dev mode — Vite proxy in vite.config.ts forwards /api/* to the real backend.
# Browser sees same-origin requests, so CORS doesn't apply.
VITE_API_BASE_URL=/api
VITE_API_PROXY_TARGET=https://api.schoolservice.in
VITE_DEFAULT_TENANT_ID=default
VITE_API_TIMEOUT=15000
VITE_API_DEBUG=true
